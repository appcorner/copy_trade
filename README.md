# CopyTrade

To start your Phoenix server:

* Run `mix setup` to install and setup dependencies
* Start Phoenix endpoint with `mix phx.server` or inside IEx with `iex -S mix phx.server`

Now you can visit [`localhost:4000`](http://localhost:4000) from your browser.

Ready to run in production? Please [check our deployment guides](https://hexdocs.pm/phoenix/deployment.html).

## Learn more

* Official website: https://www.phoenixframework.org/
* Guides: https://hexdocs.pm/phoenix/overview.html
* Docs: https://hexdocs.pm/phoenix
* Forum: https://elixirforum.com/c/phoenix-forum
* Source: https://github.com/phoenixframework/phoenix

4 ขั้นตอนนี้แบบเคร่งครัด (ห้ามข้าม) เพื่อล้างค่าทุกอย่างให้สะอาด

ขั้นตอนที่ 1: ลบ Artifacts ที่ค้างอยู่ (สำคัญมาก!) 🧹
บางครั้ง Elixir จำค่าเก่าในโฟลเดอร์ _build ทำให้มันมองไม่เห็นไฟล์ที่เราเพิ่งแก้หรือลบไป

ปิด Server (Ctrl+C ให้เรียบร้อย)

ไปที่โฟลเดอร์โปรเจกต์ ลบโฟลเดอร์ _build ทิ้งทั้งโฟลเดอร์เลยครับ

(Windows) ใช้คำสั่ง: rd /s /q _build

(หรือคลิกขวา Delete ใน File Explorer ก็ได้)

ขั้นตอนที่ 2: ตรวจสอบไฟล์ Migration ด้วยตาเปล่า 👁️
เข้าไปที่ priv/repo/migrations/ แล้วเช็คตามนี้ครับ:

ดูว่ามีไฟล์ซ้ำซ้อนไหม?

เช่นมี ..._create_trade_pairs.exs และ ..._force_fix_trade_pairs.exs อยู่พร้อมกันไหม?

ถ้ามี: ให้ลบอันเก่าทิ้ง (หรืออันที่ไม่ต้องการ) ให้เหลือแค่อันล่าสุดที่เราเขียนโค้ด create table แบบสมบูรณ์ไว้

เช็คชื่อ Module: เปิดไฟล์ดูข้างใน ห้ามมีชื่อ Module ซ้ำกัน (เช่น defmodule CopyTrade.Repo.Migrations.CreateTradePairs ห้ามมีใน 2 ไฟล์)

ขั้นตอนที่ 3: ล้าง Database แบบหมดจด (Nuclear Option) ☢️
เมื่อลบ _build และเคลียร์ไฟล์ Migration แล้ว ให้รันคำสั่งตามลำดับนี้ครับ:

PowerShell

# 1. โหลด dependencies ใหม่ (เพราะเราลบ _build ไปแล้ว)
mix deps.get

# 2. ลบ Database ทิ้ง
mix ecto.drop

# 3. สร้าง Database ใหม่
mix ecto.create

# 4. รัน Migration (คราวนี้มันจะรันใหม่ตั้งแต่ไฟล์แรกสุด)
mix ecto.migrate
⚠️ จุดสังเกต: ตอนรัน mix ecto.migrate บรรทัดสุดท้าย ต้องไม่มี Error สีแดง และควรขึ้นรายการไฟล์ที่มัน Migrate ให้เห็นครับ

ขั้นตอนที่ 4: รัน Server ใหม่
PowerShell

mix phx.server